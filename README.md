# How much to make an app

Setup

1. Install node
  ```
  brew install node
  ```
2. Run
  ```
  npm install
  ```
3. Start app (optional)
  ```
  npm run start
  ```
4. Watch changes and compile
  ```
  npm run watch
  ```